{
  "name": "crumbbeam",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Beam verified CRUMB AI context between offline devices using animated fountain-coded QR frames.",
  "license": "MIT",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0",
    "test": "node --test tests/*.test.mjs",
    "check": "npm test && npm run build"
  },
  "dependencies": {
    "qrcode": "^1.5.4",
    "zxing-wasm": "^2.0.0"
  },
  "devDependencies": {
    "@vitejs/plugin-basic-ssl": "^1.2.0",
    "vite": "^6.0.0"
  }
}
